# 🌐 Week 06: Docker Compose with .env
โปรเจกต์สร้าง Web Server โดยใช้ Docker Compose ร่วมกับไฟล์ .env

## วิธีใช้งาน
1. ตรวจสอบค่าในไฟล์ `.env`
2. รันคำสั่ง `docker compose up -d`
3. เข้าใช้งานที่ [http://localhost:8080](http://localhost:8080)
