name: ${PROJECT_NAME}
services:
  my-web-server:
    image: ${NGINX_VERSION}
    container_name: ${USER_NAME}-myweb
    ports:
      - "${WEB_PORT}:80"
    volumes:
      - ./html:/usr/share/nginx/html
